import { ConflictException, ForbiddenException, Inject, Injectable, NotFoundException, ServiceUnavailableException, UnprocessableEntityException } from '@nestjs/common'
import { createHash, randomBytes, randomUUID } from 'node:crypto'
import type { ApiUser } from '../auth/auth.types'
import { DatabaseService } from '../common/database.service'
import { assertPaymentTransition, type PaymentStatus } from './payment.rules'
import { ProviderRegistry } from './payment.provider'
import type { InitiatePaymentDto } from './payment.dto'

type PaymentRow = { id: string; public_id: string; order_id: string; order_public_id: string; order_number: string; amount_minor_units: string; currency: string; provider: string; status: PaymentStatus; payment_expires_at: string | null; provider_attempt_reference?: string | null; attempt_public_id?: string | null; attempt_status?: PaymentStatus | null }
const paymentSelect = `p.id,p.public_id,p.order_id,o.public_id AS order_public_id,o.order_number,p.amount_minor_units,p.currency,p.provider,p.status,o.payment_expires_at`

@Injectable()
export class PaymentsService {
  constructor(@Inject(DatabaseService) private readonly db: DatabaseService, private readonly providers: ProviderRegistry) {}
  private async orderForAccess(publicId: string, user: ApiUser) { const result = await this.db.query<{ id: string; public_id: string; order_number: string; user_profile_id: string | null; status: string; payment_expires_at: string | null; total_minor_units: string; currency: string }>('SELECT id,public_id,order_number,user_profile_id,status,payment_expires_at,total_minor_units,currency FROM ticketug.order WHERE public_id=$1', [publicId]); const order = result.rows[0]; if (!order) throw new NotFoundException('Order not found'); if (order.user_profile_id !== user.profileId) throw new NotFoundException('Order not found'); return order }
  async initiate(user: ApiUser, publicId: string, body: InitiatePaymentDto) {
    const order = await this.orderForAccess(publicId, user); const providerName = body.provider ?? this.providers.selected(); if (!providerName) throw new ServiceUnavailableException('PROVIDER_NOT_CONFIGURED'); const provider = this.providers.get(providerName)
    return this.db.transaction(async (client) => {
      const locked = (await client.query<typeof order>('SELECT id,public_id,order_number,user_profile_id,status,payment_expires_at,total_minor_units,currency FROM ticketug.order WHERE id=$1 FOR UPDATE', [order.id])).rows[0]
      if (locked.status === 'PAID') throw new ConflictException('PAYMENT_ALREADY_SUCCEEDED'); if (['CANCELLED','EXPIRED'].includes(locked.status)) throw new ConflictException('ORDER_EXPIRED')
      const existing = (await client.query<PaymentRow>(`SELECT ${paymentSelect},a.public_id AS attempt_public_id,a.status AS attempt_status,a.provider_attempt_reference FROM ticketug.payment p JOIN ticketug.order o ON o.id=p.order_id LEFT JOIN ticketug.payment_attempt a ON a.payment_id=p.id AND a.status IN ('PENDING','PROCESSING') WHERE p.order_id=$1 AND a.idempotency_key=$2`, [locked.id, body.idempotencyKey])).rows[0]
      if (existing) return this.present(existing)
      const paymentId = randomUUID(); const paymentPublicId = `pay_${randomBytes(12).toString('hex')}`; const attemptId = randomUUID(); const attemptPublicId = `pat_${randomBytes(12).toString('hex')}`
      await client.query(`INSERT INTO ticketug.payment(id,public_id,order_id,provider,amount_minor_units,currency) VALUES($1,$2,$3,$4,$5,$6) ON CONFLICT(order_id) DO NOTHING`, [paymentId,paymentPublicId,locked.id,providerName,locked.total_minor_units,locked.currency])
      const payment = (await client.query<{ id: string; public_id: string }>('SELECT id,public_id FROM ticketug.payment WHERE order_id=$1 FOR UPDATE', [locked.id])).rows[0]
      const inserted = await client.query(`INSERT INTO ticketug.payment_attempt(id,public_id,payment_id,provider,amount_minor_units,currency,idempotency_key,status) VALUES($1,$2,$3,$4,$5,$6,$7,'PENDING')`, [attemptId,attemptPublicId,payment.id,providerName,locked.total_minor_units,locked.currency,body.idempotencyKey])
      if (inserted.rowCount !== 1) throw new ConflictException('Duplicate payment initiation')
      const initiation = await provider.initiate({ paymentPublicId: payment.public_id, attemptPublicId, amountMinorUnits: BigInt(locked.total_minor_units), currency: locked.currency, orderReference: locked.public_id })
      await client.query('UPDATE ticketug.payment_attempt SET provider_attempt_reference=$2,provider_metadata=$3,status=$4,processing_at=now() WHERE id=$1', [attemptId, initiation.providerAttemptReference, JSON.stringify(initiation.metadata ?? {}), 'PROCESSING'])
      await client.query("UPDATE ticketug.payment SET status='PROCESSING',updated_at=now() WHERE id=$1", [payment.id])
      await client.query("UPDATE ticketug.order SET status='PAYMENT_PROCESSING', payment_state='PAYMENT_PROCESSING', updated_at=now() WHERE id=$1", [locked.id])
      return { paymentId: payment.public_id, attemptId: attemptPublicId, status: 'PROCESSING', provider: providerName, providerAttemptReference: initiation.providerAttemptReference, redirectUrl: initiation.redirectUrl, instructions: initiation.instructions, amountMinorUnits: Number(locked.total_minor_units), currency: locked.currency }
    })
  }
  async status(user: ApiUser, publicId: string) { const order = await this.orderForAccess(publicId, user); const result = await this.db.query<PaymentRow>(`SELECT ${paymentSelect},a.public_id AS attempt_public_id,a.status AS attempt_status,a.provider_attempt_reference FROM ticketug.payment p JOIN ticketug.order o ON o.id=p.order_id LEFT JOIN ticketug.payment_attempt a ON a.payment_id=p.id WHERE p.order_id=$1 ORDER BY a.initiated_at DESC LIMIT 1`, [order.id]); if (!result.rows[0]) throw new NotFoundException('PAYMENT_NOT_FOUND'); return this.present(result.rows[0]) }
  private async guestOrder(publicId: string, token: string) { const result = await this.db.query<{ id: string; public_id: string; order_number: string; user_profile_id: string | null; status: string; payment_expires_at: string | null; total_minor_units: string; currency: string; guest_access_token_hash: string }>('SELECT id,public_id,order_number,user_profile_id,status,payment_expires_at,total_minor_units,currency,guest_access_token_hash FROM ticketug.order WHERE public_id=$1', [publicId]); const order = result.rows[0]; if (!order || order.guest_access_token_hash !== createHash('sha256').update(token).digest('hex')) throw new NotFoundException('Order not found'); return order }
  async initiateGuest(publicId: string, token: string, body: InitiatePaymentDto) { const order = await this.guestOrder(publicId, token); const providerName = body.provider ?? this.providers.selected(); if (!providerName) throw new ServiceUnavailableException('PROVIDER_NOT_CONFIGURED'); const provider = this.providers.get(providerName); return this.db.transaction(async (client) => { const locked = (await client.query<typeof order>('SELECT id,public_id,order_number,user_profile_id,status,payment_expires_at,total_minor_units,currency,guest_access_token_hash FROM ticketug.order WHERE id=$1 FOR UPDATE', [order.id])).rows[0]; if (['PAID','CANCELLED','EXPIRED'].includes(locked.status)) throw new ConflictException('ORDER_EXPIRED'); const paymentId = randomUUID(); const paymentPublicId = `pay_${randomBytes(12).toString('hex')}`; const attemptId = randomUUID(); const attemptPublicId = `pat_${randomBytes(12).toString('hex')}`; await client.query(`INSERT INTO ticketug.payment(id,public_id,order_id,provider,amount_minor_units,currency) VALUES($1,$2,$3,$4,$5,$6) ON CONFLICT(order_id) DO NOTHING`, [paymentId,paymentPublicId,locked.id,providerName,locked.total_minor_units,locked.currency]); const payment = (await client.query<{ id: string; public_id: string }>('SELECT id,public_id FROM ticketug.payment WHERE order_id=$1 FOR UPDATE', [locked.id])).rows[0]; const existing = (await client.query<PaymentRow>('SELECT '+paymentSelect+',a.public_id AS attempt_public_id,a.status AS attempt_status,a.provider_attempt_reference FROM ticketug.payment p JOIN ticketug.order o ON o.id=p.order_id JOIN ticketug.payment_attempt a ON a.payment_id=p.id WHERE p.id=$1 AND a.idempotency_key=$2', [payment.id,body.idempotencyKey])).rows[0]; if (existing) return this.present(existing); await client.query(`INSERT INTO ticketug.payment_attempt(id,public_id,payment_id,provider,amount_minor_units,currency,idempotency_key,status) VALUES($1,$2,$3,$4,$5,$6,$7,'PENDING')`, [attemptId,attemptPublicId,payment.id,providerName,locked.total_minor_units,locked.currency,body.idempotencyKey]); const initiation = await provider.initiate({ paymentPublicId: payment.public_id, attemptPublicId, amountMinorUnits: BigInt(locked.total_minor_units), currency: locked.currency, orderReference: locked.public_id }); await client.query('UPDATE ticketug.payment_attempt SET provider_attempt_reference=$2,provider_metadata=$3,status=\'PROCESSING\',processing_at=now() WHERE id=$1', [attemptId,initiation.providerAttemptReference,JSON.stringify(initiation.metadata ?? {})]); await client.query("UPDATE ticketug.payment SET status='PROCESSING',updated_at=now() WHERE id=$1", [payment.id]); await client.query("UPDATE ticketug.order SET status='PAYMENT_PROCESSING',payment_state='PAYMENT_PROCESSING',updated_at=now() WHERE id=$1", [locked.id]); return { paymentId: payment.public_id, attemptId: attemptPublicId, status: 'PROCESSING', provider: providerName, providerAttemptReference: initiation.providerAttemptReference, instructions: initiation.instructions, redirectUrl: initiation.redirectUrl, amountMinorUnits: Number(locked.total_minor_units), currency: locked.currency } }) }
  async statusGuest(publicId: string, token: string) { const order = await this.guestOrder(publicId, token); const result = await this.db.query<PaymentRow>(`SELECT ${paymentSelect},a.public_id AS attempt_public_id,a.status AS attempt_status,a.provider_attempt_reference FROM ticketug.payment p JOIN ticketug.order o ON o.id=p.order_id LEFT JOIN ticketug.payment_attempt a ON a.payment_id=p.id WHERE p.order_id=$1 ORDER BY a.initiated_at DESC LIMIT 1`, [order.id]); if (!result.rows[0]) throw new NotFoundException('PAYMENT_NOT_FOUND'); return this.present(result.rows[0]) }
  private present(row: PaymentRow) { return { paymentId: row.public_id, attemptId: row.attempt_public_id, status: row.status, attemptStatus: row.attempt_status, provider: row.provider, providerAttemptReference: row.provider_attempt_reference, amountMinorUnits: Number(row.amount_minor_units), currency: row.currency, orderId: row.order_public_id, orderNumber: row.order_number } }
  async webhook(providerName: string, headers: Record<string,string|undefined>, body: unknown, rawBody: string) {
    const provider = this.providers.get(providerName); const event = provider.verifyWebhook({ headers, body, rawBody })
    return this.db.transaction(async (client) => {
      const inserted = await client.query<{ id: string }>(`INSERT INTO ticketug.webhook_event(id,provider,provider_event_id,event_type,signature_verified,payload,provider_reference) VALUES($1,$2,$3,$4,true,$5,$6) ON CONFLICT(provider,provider_event_id) DO NOTHING RETURNING id`, [randomUUID(),providerName,event.providerEventId,event.eventType,JSON.stringify(body),event.providerAttemptReference])
      if (!inserted.rows[0]) return { status: 'DUPLICATE' }
      const attempt = (await client.query<PaymentRow>(`SELECT p.id,p.public_id,p.order_id,o.public_id AS order_public_id,o.order_number,p.amount_minor_units,p.currency,p.provider,p.status,o.payment_expires_at,a.public_id AS attempt_public_id,a.status AS attempt_status,a.provider_attempt_reference FROM ticketug.payment_attempt a JOIN ticketug.payment p ON p.id=a.payment_id JOIN ticketug.order o ON o.id=p.order_id WHERE p.provider=$1 AND a.provider_attempt_reference=$2 FOR UPDATE OF p,a,o`, [providerName,event.providerAttemptReference])).rows[0]
      if (!attempt) throw new NotFoundException('Unknown provider transaction')
      if (attempt.amount_minor_units !== event.amountMinorUnits.toString()) throw new UnprocessableEntityException('INVALID_PAYMENT_AMOUNT')
      if (attempt.currency !== event.currency || event.orderReference !== attempt.order_public_id) throw new UnprocessableEntityException('INVALID_PAYMENT_CURRENCY')
      if (attempt.status === 'SUCCEEDED') { await client.query("UPDATE ticketug.webhook_event SET processing_status='DUPLICATE',processed_at=now() WHERE id=$1", [inserted.rows[0].id]); return { status: 'DUPLICATE' } }
      assertPaymentTransition(attempt.status!, event.status)
      await client.query('UPDATE ticketug.payment_attempt SET status=$2,completed_at=now() WHERE id=$1', [attempt.attempt_public_id ? (await client.query<{id:string}>('SELECT id FROM ticketug.payment_attempt WHERE public_id=$1',[attempt.attempt_public_id])).rows[0].id : '', event.status])
      await client.query('UPDATE ticketug.payment SET status=$2,successful_provider_reference=CASE WHEN $2=\'SUCCEEDED\' THEN $3 ELSE successful_provider_reference END,updated_at=now(),succeeded_at=CASE WHEN $2=\'SUCCEEDED\' THEN now() ELSE succeeded_at END WHERE id=$1', [attempt.id,event.status,event.providerAttemptReference])
      await client.query('UPDATE ticketug.order SET status=CASE WHEN $2=\'SUCCEEDED\' THEN \'PAID\' ELSE status END,payment_state=CASE WHEN $2=\'SUCCEEDED\' THEN \'PAID\' ELSE payment_state END,updated_at=now() WHERE id=$1', [attempt.order_id,event.status])
      await client.query("UPDATE ticketug.webhook_event SET processing_status='PROCESSED',processed_at=now() WHERE id=$1", [inserted.rows[0].id])
      return { status: 'PROCESSED' }
    })
  }
}
